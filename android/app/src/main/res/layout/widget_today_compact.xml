<?xml version="1.0" encoding="utf-8"?>
<!--
  4×1 layout: one line — "3 tasks · next: Title (!)" with the agent health
  folded into a colored dot before the update stamp.
-->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/widget_root"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/widget_bg"
    android:orientation="horizontal"
    android:gravity="center_vertical"
    android:paddingLeft="16dp"
    android:paddingRight="16dp"
    android:paddingTop="10dp"
    android:paddingBottom="10dp">

    <TextView
        android:id="@+id/widget_compact_line"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_weight="1"
        android:textColor="@color/widget_ink"
        android:textSize="13sp"
        android:maxLines="1"
        android:ellipsize="end"
        android:fontFeatureSettings="tnum" />

    <TextView
        android:id="@+id/widget_compact_dot"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginStart="8dp"
        android:text="●"
        android:textSize="9sp"
        android:textColor="@color/widget_faint" />

    <TextView
        android:id="@+id/widget_compact_updated"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginStart="5dp"
        android:textColor="@color/widget_faint"
        android:textSize="10sp"
        android:fontFeatureSettings="tnum" />
</LinearLayout>
